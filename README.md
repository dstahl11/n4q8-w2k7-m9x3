Static site. Live content is `data.json`.

Update: edit `data.json`, set `lastUpdated`, commit. GitHub Pages rebuilds from `main`. No build step.

Keep this repo on a random name. Do not advertise the URL.
